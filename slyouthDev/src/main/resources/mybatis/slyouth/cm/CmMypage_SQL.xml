<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cm.mypage">

	<!-- 사용자 정보 조회 -->
	<select id="mypageInfo" parameterType="CmMypageDVO" resultType="CmMypageDVO">
	<![CDATA[
		SELECT NAME 				/* 사용자이름 */
     		 , GRADE				/* 사용자기수 */
     		 , MB_TEL_NO			/* 핸드폰번호 */
     		 , EMAIL				/* 이메일 */
     	  FROM TB_CHRMGNT
 		 WHERE CHR_NO = #{chrNo}
	]]>	
	</select>
	
	<!-- 비밀번호 변경 -->
	<update id="mypagePwModify" parameterType="CmMypageDVO">
	<![CDATA[
		UPDATE TB_USER
		   SET USR_PW = #{chgUsrPw}
		 WHERE USR_ID = #{usrId}
	]]>
	</update>
</mapper>